# Return-To-Form PopSign Fresh5 Revised Scratch Architecture Input Design Review Goal Loop Prompt

Mission 3CY prompt for the Codex executor after Mission 3CX selected
`draft_revised_scratch_architecture_input_design_review_no_training`.

Read [`GOAL.md`](../../GOAL.md) first, then
[`docs/model/return-to-form-plan.md`](return-to-form-plan.md).

## Mission

Complete exactly one local/no-spend, no-training, no-implementation PopSign
fresh5 revised scratch architecture/input design review.

Use M3CX, observer 435, M3CW, and M3CR-M3CV evidence to state what a future
scratch architecture and input representation would have to change relative to
the failed `scratch_region_temporal_late_fusion_tcn_contract_v1` path before
any future implementation, training, Brev compute, export, browser activation,
model-card promotion, or product recognition claim can be considered. This
mission must not prototype, implement, train, fit, tune, mutate data, plan paid
compute, export, promote, or change final claims.

## Source Of Truth

Read in this order:

1. Latest user instruction in the current Codex thread.
2. [`GOAL.md`](../../GOAL.md), if it points at this prompt.
3. Mission 3CX strategy/downscope decision packet:
   - [`docs/validation/return-to-form-popsign-fresh5-strategy-downscope-decision-v1.json`](../validation/return-to-form-popsign-fresh5-strategy-downscope-decision-v1.json)
   - [`docs/session-logs/436-mission-3cx-popsign-fresh5-strategy-downscope.md`](../session-logs/436-mission-3cx-popsign-fresh5-strategy-downscope.md)
4. Observer 435 strategy escalation artifacts:
   - [`artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-response.md`](../../artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-response.md)
   - [`artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-prompt.md`](../../artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-prompt.md)
   - [`artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-request.json`](../../artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-request.json)
   - [`artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-raw.json`](../../artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-raw.json)
5. M3CW architecture/data generalization failure packet:
   - [`docs/validation/return-to-form-popsign-fresh5-architecture-data-generalization-failure-v1.json`](../validation/return-to-form-popsign-fresh5-architecture-data-generalization-failure-v1.json)
   - [`docs/session-logs/434-mission-3cw-popsign-fresh5-architecture-data-generalization.md`](../session-logs/434-mission-3cw-popsign-fresh5-architecture-data-generalization.md)
6. M3CR-M3CV collapse evidence:
   - [`docs/validation/return-to-form-popsign-fresh5-bounded-local-train-all-after-optimizer-loss-v1.json`](../validation/return-to-form-popsign-fresh5-bounded-local-train-all-after-optimizer-loss-v1.json)
   - [`docs/validation/return-to-form-popsign-fresh5-prediction-confidence-logit-distribution-v1.json`](../validation/return-to-form-popsign-fresh5-prediction-confidence-logit-distribution-v1.json)
   - [`docs/validation/return-to-form-popsign-fresh5-train-split-logit-feature-separability-v1.json`](../validation/return-to-form-popsign-fresh5-train-split-logit-feature-separability-v1.json)
   - [`docs/validation/return-to-form-popsign-fresh5-feature-collapse-representation-v1.json`](../validation/return-to-form-popsign-fresh5-feature-collapse-representation-v1.json)
7. Earlier PopSign fresh5 gating receipts M3CK-M3CQ, especially data/split,
   label/source, tensor/input, sampler/distribution, optimizer/loss, and tiny
   microprobe evidence.
8. Current model/train/eval code paths, read-only:
   - [`scripts/train_rawframe_model.py`](../../scripts/train_rawframe_model.py)
   - [`scripts/evaluate_rawframe_model.py`](../../scripts/evaluate_rawframe_model.py)
9. Current fail-closed claim surfaces:
   - [`web/public/model/model-card.json`](../../web/public/model/model-card.json)
   - [`docs/model/active-vocabulary-claim.json`](active-vocabulary-claim.json)
10. The return-to-form spine and tactical overlay:
    - [`docs/model/return-to-form-plan.md`](return-to-form-plan.md)

## Current Evidence

M3CR-M3CV rule out another blind fitting run under the current
`scratch_region_temporal_late_fusion_tcn_contract_v1` envelope. The 20-epoch
train-all stayed at validation `0.2`, test top-1 `0.2`, macro F1
`0.06666666666666668`, validation/test collapse to `morning: 125`, train /
validation / test logit collapse, nonseparable train features, and fused-head
representation collapse before LayerNorm/readout.

M3CK proved the current architecture/input path can train-fit a tiny
deterministic one-clip-per-label microprobe, while M3CL-M3CQ reduced the
likelihood of label-index, split leakage, source-label, tensor corruption,
sampler, evaluator, checkpoint-selection, loss, optimizer, or threshold-only
root causes.

M3CW classified the strongest current explanation as
`current_scratch_architecture_optimization_generalization_failure_under_existing_input_and_data_envelope`.
Observer 435 then recommended one strategy/downscope decision packet before any
architecture/input, training, Brev, product-claim, or STOP decision.

M3CX evaluated exactly
`revise_scratch_architecture_input_design_no_training`,
`product_downscope_no_recognition_activation`, and
`stop_scratch_recognizer_lane`, then selected exactly
`draft_revised_scratch_architecture_input_design_review_no_training`. It did
not authorize implementation, prototype, training, Brev, export, promotion,
claim changes, source expansion, pseudo-labeling, data/tensor mutation, or
final-gate weakening.

## Required Slice

Complete exactly one smallest useful no-training revised scratch
architecture/input design review.

1. Run quick state checks:

```sh
git status --short --branch
git log -10 --oneline --decorate
node scripts/audit_loop_premise.mjs --json
node scripts/audit_return_to_form_plan.mjs --json
node scripts/audit_no_pretrained_deps.mjs
node scripts/audit_no_pretrained_artifact_json.mjs
node scripts/audit_source_register.mjs
python3 -m json.tool docs/validation/return-to-form-popsign-fresh5-strategy-downscope-decision-v1.json >/dev/null
python3 -m json.tool docs/validation/return-to-form-popsign-fresh5-architecture-data-generalization-failure-v1.json >/dev/null
python3 -m json.tool artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-request.json >/dev/null
python3 -m json.tool artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-raw.json >/dev/null
```

2. Inspect existing local docs, receipts, API memo, fail-closed claim surfaces,
   and current model/train/eval code only. Do not add implementation helpers or
   run model/training/prototype code.

3. Write a tracked receipt:

`docs/validation/return-to-form-popsign-fresh5-revised-scratch-architecture-input-design-review-v1.json`

The receipt must include:

- current HEAD and inspected artifact hashes;
- source-supported observations separated from inference;
- a compact summary of the failed current architecture/input envelope;
- explicit design requirements for any future scratch architecture/input route;
- a ranked comparison of revised no-pretrained design alternatives, including
  why each differs from the failed path;
- risk matrix covering model capacity, temporal modeling, crop/region
  semantics, data volume/diversity, optimization signal, browser footprint,
  privacy/no-upload constraints, source/legal constraints, and school-deadline
  risk;
- evidence gates required before any future implementation, fitting, Brev
  compute, export, browser activation, model-card promotion, active-label
  promotion, or product recognition claim;
- explicit reasons another fitting run under the old architecture/input
  contract is not justified now;
- fallback analysis for product downscope/no-recognition and stopping the
  scratch recognizer lane;
- negative authorization proof for training/fitting, Brev, checkpoint creation,
  architecture/input implementation, tensor/manifest/source mutation,
  pretrained dependencies, export, browser activation, model-card promotion,
  final-gate weakening, unsupported claims, and push;
- proof that browser recognition remains fail-closed;
- exactly one selected next action.

4. Select exactly one next action:

- `continue_revised_scratch_architecture_input_design_review_no_training`: if
  the design review is incomplete or blocked.
- `draft_revised_scratch_architecture_input_contract_packet_no_training`: if a
  concrete revised no-pretrained design is defensible enough to justify a
  separate no-training contract/spec packet before any implementation or
  fitting.
- `draft_product_downscope_reduced_claim_plan_no_recognition`: if the design
  review cannot produce a defensible recognizer route but the product should
  continue with recognition explicitly fail-closed.
- `stop_for_human_architecture_input_scope_decision`: if the next meaningful
  step requires human approval on architecture, input representation, code
  path, scope, budget, source, label, tensor, crop, or final claims.
- `stop_scratch_recognizer_lane`: if no defensible no-pretrained scratch
  recognizer route remains inside the current school-project scope.

## Hard Boundaries

- No training, fitting, optimizer construction for fitting, backward pass,
  checkpoint creation, sweep, second local retry, fresh10 training, 75/95-label
  training, or learned auxiliary diagnostic model.
- No architecture prototype, input-representation implementation, tensor
  regeneration, crop/region-target implementation, implementation-code change,
  or helper script.
- No Brev training, spend, worker lifecycle change, sync, remote command,
  teardown, file copy, or remote planning beyond optional read-only visibility.
- No manifest, tensor, source-register, vocabulary, label-set, source import,
  media download, generated pseudo-label, source approval, or split mutation.
- No pretrained detector, landmark, backbone, embedding, model dependency,
  generated-label dependency, or pretrained-assisted data labeling.
- No ONNX export, browser model activation, active-label promotion,
  model-card promotion, final-readiness claim, final-gate weakening, product
  fallback that implies live ASL recognition, ASL correctness claim, or push.

## Acceptance Criteria

This mission can close when:

1. `GOAL.md` points at this M3CY prompt and names Mission 3CY.
2. The M3CX receipt exists and parses, or the session log records the exact
   missing-artifact blocker.
3. The observer 435 API request/raw JSON files parse and the response memo
   exists, or the session log records the exact missing-artifact blocker.
4. The M3CW and M3CR-M3CV receipts exist and parse, or the session log records
   the exact missing-artifact blocker.
5. A tracked JSON receipt exists at
   `docs/validation/return-to-form-popsign-fresh5-revised-scratch-architecture-input-design-review-v1.json`
   or the session log records the exact blocker that prevented it.
6. The receipt separates source-supported observations from inference.
7. The receipt states what must change from the failed
   `scratch_region_temporal_late_fusion_tcn_contract_v1` architecture/input
   path and what cannot be inferred from current evidence.
8. The receipt evaluates revised design alternatives, product downscope, and
   scratch-lane stop risk without approving implementation, training, Brev,
   export, promotion, source/data/tensor mutation, final-gate change, or
   browser recognition claims.
9. The receipt selects exactly one next action from this prompt.
10. Browser model claims remain fail-closed.
11. Required audits, receipt JSON validation, API artifact JSON validation, and
    `git diff --check` exit `0` or record exact blockers.
12. A numbered session log records commands, evidence, blockers, and exactly
    one next action.

## Observer Guidance

- CONTINUE if the design review is bounded, local/no-spend, no-training,
  no-implementation, evidence-backed, no-Brev, fail-closed, and selects one
  bounded next action.
- NUDGE if the receipt lacks source-vs-inference separation, a comparison to
  the failed architecture/input path, ranked design alternatives, evidence
  gates, fallback analysis, no-training/Brev/export/promotion proof,
  fail-closed claim proof, or exactly one next action.
- REDIRECT if the executor trains, fits, mutates manifests/tensors/source
  approvals, switches datasets, prototypes architecture/input changes,
  promotes a model, edits claim surfaces, runs a sweep, performs another local
  fitting attempt, or writes implementation code.
- STOP if the selected next action requires human budget, source, rights,
  annotation, crop, tensor, label, architecture/input, code-path, scope, or
  final-claim approval, or if the receipt selects `stop_scratch_recognizer_lane`.
- ESCALATE only if a new high-cost decision remains unclear after this design
  review and cannot be reduced locally without repeating prior analysis.

## Progress Ledger

Each executor session log should end with:

```text
Current state:        Mission 3CY PopSign fresh5 revised scratch architecture/input design review.
Completed:            <design-review result and receipt>.
Evidence:             <receipt, artifacts, commands>.
Remaining:            <single next action>.
Blockers:             <none or exact blocker>.
Next step:            <single next action>.
Checkpoint commit:    <commit hash>.
```
