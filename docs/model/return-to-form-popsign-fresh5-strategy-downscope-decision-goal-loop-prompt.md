# Return-To-Form PopSign Fresh5 Strategy Downscope Decision Goal Loop Prompt

Mission 3CX prompt for the Codex executor after the observer 435 strategy
escalation reduced M3CW evidence to a local/no-spend, no-training strategy /
downscope decision packet.

Read [`GOAL.md`](../../GOAL.md) first, then
[`docs/model/return-to-form-plan.md`](return-to-form-plan.md).

## Mission

Complete exactly one local/no-spend, no-training, no-implementation PopSign
fresh5 strategy/downscope decision packet.

Use the completed M3CW failure packet and observer 435 API memo to decide
whether the next defensible direction is a revised scratch architecture/input
design review, a product downscope/reduced-claim plan with recognition still
fail-closed, or stopping the scratch recognizer lane. This mission must not
train, fit, tune, spend Brev, prototype architecture/input changes, mutate data,
write implementation code, export, browser-activate, promote a model card, or
change final claims.

## Source Of Truth

Read in this order:

1. Latest user instruction in the current Codex thread.
2. [`GOAL.md`](../../GOAL.md), if it points at this prompt.
3. Observer 435 strategy escalation artifacts:
   - [`artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-response.md`](../../artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-response.md)
   - [`artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-prompt.md`](../../artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-prompt.md)
   - [`artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-request.json`](../../artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-request.json)
   - [`artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-raw.json`](../../artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-raw.json)
4. M3CW architecture/data generalization failure packet:
   - [`docs/validation/return-to-form-popsign-fresh5-architecture-data-generalization-failure-v1.json`](../validation/return-to-form-popsign-fresh5-architecture-data-generalization-failure-v1.json)
   - [`docs/session-logs/434-mission-3cw-popsign-fresh5-architecture-data-generalization.md`](../session-logs/434-mission-3cw-popsign-fresh5-architecture-data-generalization.md)
5. M3CR-M3CV collapse evidence:
   - [`docs/validation/return-to-form-popsign-fresh5-bounded-local-train-all-after-optimizer-loss-v1.json`](../validation/return-to-form-popsign-fresh5-bounded-local-train-all-after-optimizer-loss-v1.json)
   - [`docs/validation/return-to-form-popsign-fresh5-prediction-confidence-logit-distribution-v1.json`](../validation/return-to-form-popsign-fresh5-prediction-confidence-logit-distribution-v1.json)
   - [`docs/validation/return-to-form-popsign-fresh5-train-split-logit-feature-separability-v1.json`](../validation/return-to-form-popsign-fresh5-train-split-logit-feature-separability-v1.json)
   - [`docs/validation/return-to-form-popsign-fresh5-feature-collapse-representation-v1.json`](../validation/return-to-form-popsign-fresh5-feature-collapse-representation-v1.json)
6. Earlier PopSign fresh5 gating receipts M3CK-M3CQ, especially the data/split,
   label/source, tensor/input, sampler/distribution, and optimizer/loss packets.
7. Current fail-closed claim surfaces:
   - [`web/public/model/model-card.json`](../../web/public/model/model-card.json)
   - [`docs/model/active-vocabulary-claim.json`](active-vocabulary-claim.json)
8. The return-to-form spine and tactical overlay:
   - [`docs/model/return-to-form-plan.md`](return-to-form-plan.md)

## Current Evidence

M3CW classified the strongest current explanation as
`current_scratch_architecture_optimization_generalization_failure_under_existing_input_and_data_envelope`.
It found the current scratch `scratch_region_temporal_late_fusion_tcn_contract_v1`
lane can train-fit a tiny deterministic microprobe but does not learn
label-separating representations across the full balanced 125-clip train split.

M3CR-M3CV evidence rules out another blind fitting run: the 20-epoch train-all
stayed at validation `0.2`, test top-1 `0.2`, macro F1
`0.06666666666666668`, validation/test collapse to `morning: 125`, train /
validation / test logit collapse, nonseparable train features, and fused-head
representation collapse before LayerNorm/readout.

The observer 435 API memo authorizes only one local/no-spend, no-training
strategy/downscope decision packet. It does not authorize training, Brev,
export, browser activation, final-gate weakening, source expansion, data/tensor
mutation, architecture/input implementation, or product recognition claims.

## Required Slice

Complete exactly one smallest useful no-training strategy/downscope decision
packet.

1. Run quick state checks:

```sh
git status --short --branch
git log -10 --oneline --decorate
node scripts/audit_loop_premise.mjs --json
node scripts/audit_return_to_form_plan.mjs --json
node scripts/audit_no_pretrained_deps.mjs
node scripts/audit_no_pretrained_artifact_json.mjs
node scripts/audit_source_register.mjs
python3 -m json.tool docs/validation/return-to-form-popsign-fresh5-architecture-data-generalization-failure-v1.json >/dev/null
python3 -m json.tool artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-request.json >/dev/null
python3 -m json.tool artifacts/research/observer-435-popsign-fresh5-post-collapse-strategy-api-raw.json >/dev/null
```

2. Inspect existing local docs, receipts, API memo, and claim surfaces only. Do
   not add implementation helpers or run model/training/prototype code.

3. Write a tracked receipt:

`docs/validation/return-to-form-popsign-fresh5-strategy-downscope-decision-v1.json`

The receipt must include:

- current HEAD and inspected artifact hashes;
- source-supported observations separated from inference;
- a summary of M3CR/M3CT/M3CU/M3CV collapse and feature-separability evidence;
- an explanation of why another fitting run is not justified now;
- a restatement of no-pretrained, browser-first, fail-closed, no-upload, and
  source/legal constraints;
- explicit evaluation of exactly these three options:
  - `revise_scratch_architecture_input_design_no_training`;
  - `product_downscope_no_recognition_activation`;
  - `stop_scratch_recognizer_lane`;
- exactly one selected next action;
- negative authorization proof for training/fitting, Brev, checkpoint creation,
  export, browser activation, model-card promotion, final-gate weakening,
  source expansion/import, pseudo-labeling, tensor/manifest/split/label
  mutation, and implementation-code changes;
- proof that browser recognition remains fail-closed.

4. Select exactly one next action:

- `draft_revised_scratch_architecture_input_design_review_no_training`: if a
  revised no-training design review is still the most defensible next slice.
- `draft_product_downscope_reduced_claim_plan_no_recognition`: if the project
  should pivot toward a useful ASL learning product with recognition explicitly
  fail-closed.
- `stop_scratch_recognizer_lane`: if no defensible no-pretrained scratch
  recognizer route remains inside the current school-project scope.

## Hard Boundaries

- No training, fitting, optimizer construction for fitting, backward pass,
  checkpoint creation, sweep, second local retry, fresh10 training, or 75/95
  label training.
- No architecture prototype, input-representation implementation, tensor
  regeneration, crop/region-target implementation, or implementation-code
  change.
- No optimized auxiliary classifier, threshold-tuning route, or learned
  diagnostic model.
- No Brev training, spend, worker lifecycle change, sync, remote command,
  teardown, file copy, or remote planning beyond optional read-only visibility.
- No manifest, tensor, source-register, vocabulary, label-set, source import,
  media download, generated pseudo-label, or source approval mutation.
- No pretrained detector, landmark, backbone, embedding, model dependency, or
  generated-label dependency.
- No ONNX export, browser model activation, active-label promotion,
  model-card promotion, final-readiness claim, final-gate weakening, product
  fallback detour that implies live ASL recognition, ASL correctness claim, or
  push.

## Acceptance Criteria

This mission can close when:

1. `GOAL.md` points at this M3CX prompt and names Mission 3CX.
2. The observer 435 API request/raw JSON files parse and the response memo
   exists, or the session log records the exact missing-artifact blocker.
3. The M3CW receipt exists and parses, or the session log records the exact
   missing-artifact blocker.
4. A tracked JSON receipt exists at
   `docs/validation/return-to-form-popsign-fresh5-strategy-downscope-decision-v1.json`
   or the session log records the exact blocker that prevented it.
5. The receipt separates source-supported observations from inference.
6. The receipt evaluates exactly the three allowed options and selects exactly
   one next action from this prompt.
7. The receipt proves no training/fitting/checkpoint/Brev spend or lifecycle/
   source mutation/manifest mutation/tensor mutation/pretrained dependency/
   implementation-code change/export/browser activation/model-card promotion/
   final-gate action/unsupported claim/push occurred.
8. Browser model claims remain fail-closed.
9. Required audits, receipt JSON validation, API artifact JSON validation, and
   `git diff --check` exit `0` or record exact blockers.
10. A numbered session log records commands, evidence, blockers, and exactly
    one next action.

## Observer Guidance

- CONTINUE if the packet is bounded, local/no-spend, no-training,
  evidence-backed, no-Brev, and selects one bounded next action.
- NUDGE if the receipt lacks the three-option analysis, source-vs-inference
  split, artifact hashes, no-training/Brev/export/promotion proof, fail-closed
  claim proof, or exactly one next action.
- REDIRECT if the executor trains, fits, mutates manifests/tensors/source
  approvals, switches datasets, prototypes architecture/input changes, promotes
  a model, edits claim surfaces, runs a sweep, performs a second local
  train-all attempt, or writes implementation code.
- ESCALATE only if the strategy/downscope decision is still unclear after this
  packet and the cost of choosing wrong is high.
- STOP if the selected next action requires human budget, source, rights,
  annotation, crop, tensor, label, architecture/input, code-path, scope, or
  final-claim approval.

## Progress Ledger

Each executor session log should end with:

```text
Current state:        Mission 3CX PopSign fresh5 strategy/downscope decision packet.
Completed:            <decision result and receipt>.
Evidence:             <receipt, API memo, artifacts, commands>.
Remaining:            <single next action>.
Blockers:             <none or exact blocker>.
Next step:            <single next action>.
Checkpoint commit:    <commit hash>.
```
