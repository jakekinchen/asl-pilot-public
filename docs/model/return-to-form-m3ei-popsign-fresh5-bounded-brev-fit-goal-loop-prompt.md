# Return-To-Form M3EI PopSign Fresh5 Bounded Brev Fit Goal Loop Prompt

## Status

Superseded by Mission 3EJ and the follow-on Tiny2/Tiny3 gated proof path.

This file is retained as historical context because earlier M3EH/M3EH-R
steering referred to it. It must not be used as autonomous authorization for a
broad PopSign Fresh5 timed training run.

## Superseding Decision

The GPT Pro strategy result and local evidence changed the plan:

- M3DU proved remote CUDA train/eval plumbing but produced weak,
  non-promotable metrics.
- M3EF failed train sanity for both materialized-region and full-frame inputs
  and showed collapsed predictions.
- M3EG blocked another autonomous training-style retry pending strategy review.
- The supervising user asked to make the successful path explicit in repo
  goals and milestones.

Therefore a future M3EI-style run is allowed only after a replacement prompt
rewrites it as a Tiny2/Tiny3 gated signal proof. The correct follow-on prompt is:

[`docs/model/return-to-form-m3ek-tiny2-tiny3-gated-proof-preparation-goal-loop-prompt.md`](return-to-form-m3ek-tiny2-tiny3-gated-proof-preparation-goal-loop-prompt.md)

## Hard Boundary

Do not run the timed Fresh5 training command from the former version of this
prompt. Do not spend Brev, train, evaluate, copy back model artifacts, export,
promote, activate browser recognition, or make recognition claims from this
historical file.

## Required Redirect

If `GOAL.md`, `docs/model/return-to-form-plan.md`, an observer handoff, or an
old receipt points here, the observer must REDIRECT to one of:

- Mission 3EJ if the strategy steering receipt has not been written.
- Mission 3EK if strategy steering is complete and the next step is Tiny2/Tiny3
  label/source/gate preparation.
- A fail-closed MVP package refresh if the deadline product path is the next
  priority.

## Preserved Historical Context

The old intent was a single bounded PopSign Fresh5 fit after M3EH proved worker,
CUDA, repo/data sync, and remote dry-runs. That old intent is now considered too
broad for the current evidence. Any future recognizer compute must first pass:

- source/licensing receipts for selected labels;
- deliberately separable Tiny2/Tiny3 label selection;
- one-batch overfit at least `0.95` train accuracy;
- label-shuffle negative control failure;
- no class collapse;
- signer-held-out evaluation plan;
- open-set/hard-negative threshold plan;
- browser export/parity instrumentation;
- explicit abort gates and promotion gates.

The product remains fail-closed unless a future browser artifact passes its own
promotion receipt.
