# Return-To-Form M3FT Fail-Closed Product Polish No Recognition Goal Loop Prompt

Mission 3FT prompt for the Codex executor after Mission 3FS found no local
Detector 0 no-training repair and observer 597 API research recommended a
fail-closed product-polish redirect.

Read [`GOAL.md`](../../GOAL.md) first, then
[`docs/model/return-to-form-plan.md`](return-to-form-plan.md).

## Mission

Perform one local/no-spend/no-training fail-closed product/claim-hygiene slice.
The goal is to improve, or verify no need to improve, the browser/app/docs
surface that tells users recognition is unavailable/not trained, no active
vocabulary is promoted, no raw learner video upload is used during normal
practice, and Detector 0 evidence remains research-only.

This is a one-slice autonomous safety/product redirect, not a permanent
decision to abandon the ML lane. Further Detector 0, dataset, crop/input,
architecture, or compute work requires a separate human-approved strategy and
compute/source receipt because M3FS did not identify a local no-training repair.

This prompt does not authorize Detector 0 work, smoke rerun, training/fitting,
evaluator rerun, checkpoint creation, Brev lifecycle/spend, remote execution,
source/media import, source-register mutation, manifest/tensor/packet/
vocabulary mutation, export, promotion, browser recognition activation, runtime
Detector 0 authority, model-card promotion, active-vocabulary promotion,
final-readiness claim, product-readiness claim, trainability claim, ASL
correctness claim, or push.

## Source Of Truth

Read in this order:

1. Latest supervising-user instruction in the current Codex thread.
2. [`GOAL.md`](../../GOAL.md), if it points at this prompt.
3. Observer 597 research artifacts:
   - [`artifacts/research/observer-597-m3fs-detector0-strict-gate-strategy/response.md`](../../artifacts/research/observer-597-m3fs-detector0-strict-gate-strategy/response.md)
   - [`docs/session-logs/597-observer-escalate-m3fs-fail-closed-product-polish-handoff.md`](../session-logs/597-observer-escalate-m3fs-fail-closed-product-polish-handoff.md)
4. M3FS metric-triage receipt and session log:
   - [`docs/validation/return-to-form-m3fs-detector0-strict-gate-metric-triage-no-brev-v1.json`](../validation/return-to-form-m3fs-detector0-strict-gate-metric-triage-no-brev-v1.json)
   - [`docs/session-logs/596-mission-3fs-detector0-strict-gate-metric-triage-no-brev.md`](../session-logs/596-mission-3fs-detector0-strict-gate-metric-triage-no-brev.md)
5. Current product and claim surfaces:
   - [`web/src/components/PracticeApp.tsx`](../../web/src/components/PracticeApp.tsx)
   - [`web/src/components/LessonApp.tsx`](../../web/src/components/LessonApp.tsx)
   - [`web/src/app/validation/page.tsx`](../../web/src/app/validation/page.tsx)
   - [`web/public/model/model-card.json`](../../web/public/model/model-card.json)
   - [`web/public/model/detector0-card.json`](../../web/public/model/detector0-card.json)
   - [`docs/model/active-vocabulary-claim.json`](active-vocabulary-claim.json)
   - [`web/public/model/claim-matrix.json`](../../web/public/model/claim-matrix.json)
   - [`docs/validation/final-claim-matrix.json`](../validation/final-claim-matrix.json)
6. Existing repo audits and smoke scripts under [`scripts/`](../../scripts/).

## Starting Evidence

- M3FS executor commit `1313008` completed one local/no-spend/no-training
  strict-gate metric triage and selected `continue_openai_or_gpt_pro_research`.
- M3FS found no artifact/accounting/report availability issue and no strict-gate
  contract or threshold-accounting bug.
- M3FS classified the Detector 0 strict-gate evidence as weak diagnostic
  packet-frame evidence with packet-frame sample-size limits, target-schema
  limits, crop/input representation limits, model/architecture/training-budget
  limits, and no browser/runtime authority.
- Browser/model claim surfaces remain fail-closed:
  `web/public/model/model-card.json` is `status: "not_trained"`,
  `web/public/model/detector0-card.json` is `status: "not_trained"` with
  `promotion_state: "research_only"` and `browser_artifact: null`, and
  `docs/model/active-vocabulary-claim.json` has `activeLabels: []`.
- Observer 597 API research recommended
  `redirect_to_fail_closed_product_polish_no_recognition`: one bounded product
  slice to verify or improve recognition-unavailable UX and claim hygiene while
  keeping Detector 0 evidence diagnostic only.
- Prior fail-closed product evidence exists from M3FN/M3FO, so M3FT must be a
  narrow current-state polish or no-op verification slice, not a broad rebuild.

## Required Slice

Complete one smallest useful fail-closed product/claim-hygiene step:

1. Run baseline checks:

```sh
git status --short --branch
git log -12 --oneline --decorate
node scripts/audit_loop_premise.mjs --json
node scripts/audit_return_to_form_plan.mjs --json
node scripts/audit_no_pretrained_deps.mjs
node scripts/audit_no_pretrained_artifact_json.mjs
node scripts/audit_source_register.mjs
python3 -m json.tool docs/validation/return-to-form-m3fs-detector0-strict-gate-metric-triage-no-brev-v1.json >/dev/null
python3 -m json.tool web/public/model/model-card.json >/dev/null
python3 -m json.tool web/public/model/detector0-card.json >/dev/null
python3 -m json.tool docs/model/active-vocabulary-claim.json >/dev/null
python3 -m json.tool web/public/model/claim-matrix.json >/dev/null
python3 -m json.tool docs/validation/final-claim-matrix.json >/dev/null
brev ls --json
git diff --check
```

2. Inspect current browser/app/docs copy and claim surfaces for language that
   could imply active ASL recognition, trained labels, Detector 0 runtime
   authority, ASL correctness, or final readiness while the model remains
   `not_trained`.

3. Run the strongest relevant existing fail-closed product/status checks that
   fit the current local environment, for example:

```sh
node scripts/audit_practice_screen_contract.mjs
node scripts/audit_practice_scope_copy_smoke.mjs
node scripts/audit_practice_progress_smoke.mjs
node scripts/audit_practice_camera_behavior_smoke.mjs
node scripts/audit_lesson_page_smoke.mjs
node scripts/audit_lesson_fail_closed.mjs
node scripts/audit_avatar_no_recognition_claims.mjs
node scripts/audit_validation_page_smoke.mjs
node scripts/audit_final_claim_matrix.mjs
node scripts/audit_no_raw_video_upload.mjs
npm --prefix web run typecheck
npm --prefix web run lint
```

If a browser/runtime smoke cannot run because of local browser, auth, camera, or
permission state, record the exact blocker and run the adjacent static audit.
Do not hide runtime blockers.

4. If the checks expose one small product/claim-hygiene blocker, fix exactly
   one bounded issue, such as stale recognition-unavailable copy, validation
   status mismatch, a route/status affordance that implies recognition is active,
   or a claim-matrix/docs mismatch. Keep the fix scoped.

5. If no small blocker is found, write a no-op verification receipt explaining
   that current surfaces already preserve fail-closed recognition-unavailable
   claims.

## Receipt

Write:

`docs/validation/return-to-form-m3ft-fail-closed-product-polish-no-recognition-v1.json`

The receipt must include:

- current commit and active prompt;
- observer 597 research artifact references;
- local checks and fail-closed claim-surface status;
- read-only Brev default-off state;
- product/UI/docs/claim surfaces inspected;
- exact changed files and why, or exact no-op reason;
- any runtime/browser/audit blockers;
- explicit statement that M3FQ/M3FR/M3FS Detector 0 evidence remains
  diagnostic-only and is not browser/runtime authority;
- explicit statement that no training, fitting, evaluator rerun, smoke rerun,
  checkpoint creation, Brev lifecycle/exec/sync/copy/spend, source/media
  import, source/manifest/tensor/packet/vocabulary mutation, export,
  promotion, browser activation, model-card or active-vocabulary promotion,
  push, or unsupported claim occurred;
- `pretrained_components: []`;
- changed files;
- exactly one next action.

Allowed next actions:

- `continue_fail_closed_product_polish_no_recognition` if another bounded
  fail-closed product/claim-hygiene issue remains.
- `continue_single_fail_closed_claim_hygiene_fix` if one concrete claim-surface
  mismatch remains and can be fixed locally without ML/data/runtime authority.
- `draft_final_fail_closed_demo_evidence_package` if the current product/status
  surfaces are ready for final evidence packaging.
- `stop_for_human_demo_acceptance_review` if the fail-closed product surface is
  complete enough and the next decision is human demo/submission review.
- `stop_for_human_product_scope_review` if further changes require human UX,
  scope, claim, or submission tradeoff decisions.

## Session Log

Write:

`docs/session-logs/598-mission-3ft-fail-closed-product-polish-no-recognition.md`

The session log must record commands, evidence inspected, product/claim hygiene
result, Brev default-off status, changed files, and exactly one next action.

## Boundaries

- Local/no-spend/no-training only.
- No Detector 0 smoke rerun, recognizer smoke, evaluator run, local or remote
  training command, fitting, backward pass, optimizer step, checkpoint creation,
  architecture search, threshold tuning for promotion, export, model-card
  promotion, active-vocabulary promotion, browser recognition activation,
  product-runtime recognition mutation, runtime Detector 0 authority,
  final-readiness claim, trainability claim, or positive ASL correctness claim.
- No Brev start/exec/sync/copy/stop, remote dry-run, remote training, package
  install, duplicate worker, or GPU/cloud spend.
- No source-register edit, source/media import, manifest/tensor/packet/
  vocabulary mutation, raw learner video/frame upload, push, amend,
  destructive reset, or no-verify commit.
- No pretrained detector, landmark model, backbone, embedding, teacher logits,
  MediaPipe, OpenPose, YOLO, SAM, DINO, CLIP, `from_pretrained`,
  `pretrained=True`, pseudo-labels, generated labels, or machine-generated
  landmarks in the promoted lane.
- Do not create a new parallel audit system.

## Acceptance Criteria

This mission can close when:

1. `GOAL.md` points at this prompt and names Mission 3FT.
2. Required local checks pass or record exact blockers.
3. Product/UI/docs/claim surfaces are inspected for recognition-unavailable
   clarity and unsupported claim risk.
4. Any implementation change is limited to one bounded fail-closed product or
   claim-hygiene issue, or the receipt records a no-op verification.
5. Claim surfaces remain fail-closed:
   - `web/public/model/model-card.json` remains `status: "not_trained"`;
   - `web/public/model/detector0-card.json` remains `status: "not_trained"`,
     `promotion_state: "research_only"`, and `browser_artifact: null`;
   - `docs/model/active-vocabulary-claim.json` keeps `activeLabels: []`.
6. No forbidden ML, Brev, source, export, promotion, browser activation,
   runtime Detector 0 authority, or unsupported claim work occurred.
7. A tracked receipt and numbered session log exist and select exactly one next
   action.

## Observer Guidance

- CONTINUE if the executor keeps the work local/no-spend/no-training, improves
  or verifies fail-closed product/claim hygiene, preserves claim surfaces, and
  selects one allowed next action.
- NUDGE if it misses current claim-surface proof, unsupported-claim checks,
  runtime/audit blocker accounting, forbidden-action proof, changed-file
  accounting, or exactly one next action.
- REDIRECT if it attempts ML/data/Brev/export/promotion/browser activation,
  source import, gate weakening, or claim expansion.
- STOP if the final fail-closed product surface is ready for human demo or
  further changes require human scope/claim/submission decisions.
- ESCALATE only if the next decision would materially change product claims,
  privacy posture, architecture, source scope, or submission strategy.
